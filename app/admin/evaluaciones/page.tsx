'use client';
import {useState} from 'react'; import {exams} from '@/lib/data'; import {useDemo} from '@/app/providers'; import {Icon} from '@/lib/icons';
export default function AdminExams(){const {courses}=useDemo();const [open,setOpen]=useState(false);const [toast,setToast]=useState(false);const [question,setQuestion]=useState('¿Cuál es el objetivo principal de este módulo?');const save=()=>{setOpen(false);setToast(true);setTimeout(()=>setToast(false),2200)};return <div className="dash-page"><div className="dash-head"><div><span className="eyebrow">Evaluación</span><h1>Evaluaciones</h1><p>Configura cuestionarios finales y puntajes mínimos.</p></div><button className="btn btn-primary" onClick={()=>setOpen(v=>!v)}><Icon name="plus"/> Crear evaluación</button></div>{open&&<div className="panel" style={{marginBottom:18}}><h2>Nueva evaluación</h2><div className="form-row"><div className="field"><label>Curso</label><select>{courses.slice(0,6).map(c=><option key={c.id}>{c.title}</option>)}</select></div><div className="field"><label>Puntaje mínimo</label><input defaultValue="70" type="number"/></div></div><div className="field"><label>Pregunta</label><input value={question} onChange={e=>setQuestion(e.target.value)}/></div><div className="form-row"><div className="field"><label>Respuesta A</label><input defaultValue="Fortalecer la comprensión del contenido"/></div><div className="field"><label>Respuesta B</label><input defaultValue="Opción distractora"/></div></div><div className="form-row"><div className="field"><label>Respuesta C</label><input defaultValue="Opción distractora"/></div><div className="field"><label>Respuesta D</label><input defaultValue="Opción distractora"/></div></div><div className="field"><label>Respuesta correcta</label><select defaultValue="A"><option>A</option><option>B</option><option>C</option><option>D</option></select></div><button className="btn btn-soft"><Icon name="plus"/> Añadir pregunta</button> <button className="btn btn-primary" onClick={save}>Guardar evaluación</button></div>}<div className="table-wrap"><table className="data-table"><thead><tr><th>Curso</th><th>Preguntas</th><th>Puntaje mínimo</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>{courses.slice(0,6).map((c,i)=><tr key={c.id}><td className="table-title">{c.title}</td><td>{exams.find(e=>e.courseSlug===c.slug)?.questions.length||10}</td><td>{exams.find(e=>e.courseSlug===c.slug)?.passingScore||70}%</td><td><span className="badge status-ok">Activa</span></td><td><button className="icon-btn"><Icon name="edit" size={15}/></button></td></tr>)}</tbody></table></div>{toast&&<div className="toast">✓ Evaluación guardada en la demo</div>}</div>}
